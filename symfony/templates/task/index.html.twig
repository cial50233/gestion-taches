<!DOCTYPE html>
<html>
<head>
    <title>Mes tâches</title>
</head>
<body>

    <h1>Liste des tâches</h1>

    <ul>
        {% for task in tasks %}
            <li>
                {{ task.name }}

                {% if task.completed %}
                    ✅
                {% else %}
                    ❌
                {% endif %}
            </li>
        {% else %}
            <li>Aucune tâche pour le moment</li>
        {% endfor %}
    </ul>
</body>
</html>